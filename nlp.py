
class Parser:

    def __init__(self):
        return

    def parse(self, inp):
        """this should take a phrase and return a list containing a command in spot 0, and an object/feature/person id in
        spot 1 and potentially 2"""
        return [inp]

    def find_killer(self, killer):
        """this should take a phrase and return either the killers name or 'WRONG'"""
        return 'WRONG'

    def find_weapon(self, weapon):
        """this should take a phrase and return either 'CANDLESTICK' or 'WRONG'"""
        return 'WRONG'
